/* eslint-disable no-unused-vars */
/* eslint-disable @typescript-eslint/no-unused-vars */
const bookService = async (bookingData, bookingUser) => {
  //todo: based on booing user role i.e. admin or user => create booking
};

const getBookings = async (bookingUser) => {
  //todo: based on booing user role i.e. admin or user => get bookings
};

export const bookingService = {
  bookService,
  getBookings,
};
